# Bubble Tea terminal UI

Phant 2.0 will use Bubble Tea for its interactive terminal UI. This keeps the application in Go, lets the TUI directly reuse the existing Go core, and supports a session-scoped dump collector running while users navigate between views.
